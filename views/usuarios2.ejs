<%-include("template/cabecera.ejs", {tituloWeb: 'Pagina de inicio'}) %>

    <div class = "container">ejeseupelon

            <table class="table">
                <thead>
                  <tr>
                    <th scope="col">#</th>
                    <th scope="col">Nombre</th>
                    <th scope="col">Descripcion</th>
                    <th scope="col">Accion</th>
                  </tr>
                </thead>
                <tbody>
                    <% if (arrayMascotas.length > 0) { %>

                        <% var cont = 0;
                           arrayMascotas.forEach(mascota => {%>
                            <tr>
                                <th scope="row"><%= mascota.IdUsuario %> </th>
                                <td><%= mascota.ContrasenaUsuario%></td>
                                <td><%= mascota.NombreUsuario%></td>
                                <td>
                                  <a href="/database/buscar_usuario/<%= mascota.IdUsuario%>" class="btn btn-warning btn-sm">Editar</a>
                                </td>
                                <td>
                                  <a href="/database/borrar_usuario/<%= mascota.IdUsuario%>" class="btn btn-warning btn-sm">Borrar</a>
                                </td>

                                <td>@mdo</td>
                            </tr>


                      <%cont++; }) %>
                    <% } %>

                </tbody>
              </table>

    </div>
<%-include("template/footer.ejs") %>